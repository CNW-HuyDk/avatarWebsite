function changeImage(imageSrc) {
    var bigImage = document.getElementById('bigImage');
    bigImage.src = imageSrc;
}

function changeImageAo(imageSrc) {
    var bigImageAo = document.getElementById('bigImageAo');
    bigImageAo.src = imageSrc;
}

function changeImageMat(imageSrc) {
    var bigImageMat = document.getElementById('bigImageMat');
    bigImageMat.src = imageSrc;
}

function changeImagPK(imageSrc) {
    var bigImagePK = document.getElementById('bigImagePK');
    bigImagePK.src = imageSrc;
}

function changeImageTay(imageSrc) {
    var bigImageTay = document.getElementById('bigImageTay');
    bigImageTay.src = imageSrc;
}

function changeImageToc(imageSrc) {
    var bigImageToc = document.getElementById('bigImageToc');
    bigImageToc.src = imageSrc;
}